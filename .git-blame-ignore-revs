# Revisions that changed formatting and nothing else. `git blame` skips them,
# so the line you find is the commit that wrote the line rather than the one
# that reindented it.
#
# GitHub reads this file natively. Locally, once:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs

# Format the tree with rustfmt, once, so a gate can hold it
a6572f796894b2dbf4ede7f6a75fdd6f18f8e7d8
