# AttaCore 开发约定

## 1. 代码注释

**只保留文件头部的概要说明。**

文件头注释回答"这个文件是什么、在整体里承担什么角色"——一个不熟悉这块的人读完能知道该不该继续往下读。写到这个程度就够了。

**能从代码本身读出来的，不要用注释再讲一遍。**

具体地说，以下都不写：

- 逐行/逐段复述控制流（"先检查 X，然后遍历 Y"）
- 复述函数签名、参数含义、返回值类型
- 复述结构体字段的名字所表达的意思
- 标注章节分隔（`// ---- 初始化 ----`）
- 记录改动过程（"以前这里是 A，改成了 B"、"某某审计发现的问题"、"TS parity: xxx.ts"）

最后一类要特别说明：**变更叙事属于 git，不属于代码。** "为什么以前那样是错的"写进 commit message，`git blame` 就能找回来；写在代码里只会随着下一次改动变成谎话。

真正需要写进代码的只有一种情况：**读者看着这段代码会合理地认为它写错了，而它没错。** 比如一个反直觉的顺序依赖、一个绕开常规写法的性能取舍、一个上游 API 的坑。这种注释解释的是"为什么不能改成看起来更自然的那种写法"，落在具体那一行上，一两句话说完。

命名和结构是第一位的表达手段。如果一段代码需要注释才能读懂，先考虑改名或拆函数。

## 2. 中间过程文档不入库

**状态分析、对比分析、审计报告、评估报告这类文档，不提交到 git。**

判断标准是**这份文档描述的是"系统怎么构建的"还是"系统当时什么样"**：

| 提交 | 不提交 |
|---|---|
| 架构设计、特性设计 | 状态评估、现状快照 |
| 使用手册、编写规范 | 竞品对比、对齐差距分析 |
| 接口/协议/配置规范 | 审计报告、缺陷清单 |
| 测试架构说明 | 重构前后对比、迁移进度跟踪 |

理由很直接：前者描述的东西相对稳定，后者一落盘就开始过期，而过期的状态文档比没有更糟——它会被当成事实引用。这类分析的价值在于**当下驱动决策**，决策落成代码和 commit message 之后，文档本身就没用了。

**产出位置**：写到工作区之外的临时目录（会话 scratchpad、`/tmp`）。确实需要落在仓库里的，放 `.local/`（已在 `.gitignore` 中）。

**结论去哪**：分析得出的结论要落成三样东西之一——改代码、写进 commit message、或者进 `docs/` 的设计文档。**不要留一份报告等着别人去读。**

`.claude/skills/` 下的 `atta-status`、`atta-compare` 这两个 skill 天然产出这类文档，用它们时尤其注意输出位置。

## 3. docs/ 目录

`docs/` 只放两类东西：

- **设计与架构说明** —— 系统怎么组织的、为什么这么组织
- **使用手册与编写规范** —— 怎么配置、怎么写 skill / agent / 测试

不属于这两类的，按 §2 处理。

文档被代码注释引用是常态，删除或重命名前先 `grep` 一遍引用点，别留悬空链接。（曾经有 9 处指向不存在文档的引用，其中 `docs/RUST_ARCHITECTURE.md` 被 `crates/permissions/src/gate.rs` 当作权威依据引用了 9 次——现已清干净，`.rs` 与 `docs/` 里现在没有悬空引用。）
