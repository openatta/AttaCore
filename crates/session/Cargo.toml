[package]
name = "session"
version      = { workspace = true }
edition      = { workspace = true }
rust-version = { workspace = true }
license      = { workspace = true }
publish      = false
description  = "AttaCore — in-memory session state management"

[dependencies]
base          = { workspace = true }
history       = { workspace = true }
# For the single shared tiktoken-based token estimator (`model::tokens`) —
# `token_count()` must agree with the compaction path's estimate.
model         = { workspace = true }
tokio         = { workspace = true }
time          = { workspace = true }

thiserror = { workspace = true }
uuid = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
async-trait = { workspace = true }
