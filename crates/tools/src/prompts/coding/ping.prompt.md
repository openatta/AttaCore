Ping a URL via HTTP HEAD and return the latency in milliseconds.
- Use this to check if a server is reachable and measure response time.
- Input: {"url": "https://example.com/health", "label": "optional note"}
- Output: "[latency: 123 ms | HTTP 200]" (or an error message)
