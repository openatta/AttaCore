# 测试用例：Research 场景 —— 引用格式 + 笔记落盘
# 前置: 无（--scene research）
# 预期: 第一轮对比两段内联给出的材料，回答应带编号引用 + 来源列表（场景系统
#       提示词 citation_guidance 的行为）；第二轮要求把结论写入笔记文件，模型
#       应该调用 Write 工具（B3 的"边调研边记笔记"缓解措施）。
#       WebSearch 目前没有真实后端（见 register_builtin_tools 的说明），本用例
#       故意不依赖它，材料直接内联给出。
#       注意：本用例是默认的逐轮独立 session（`test-turn-{turn.index}`），
#       轮之间没有对话记忆延续——只有同一个 tmp 工作目录（文件系统状态）跨轮
#       共享。所以第二轮不能写"把刚才的结论写下来"这种依赖对话记忆的话术，
#       必须把要写的内容在本轮里说完整（第一版这里踩了这个坑，模型正确地说
#       "看不到之前的对话"而不是瞎编，行为是对的，是用例设计的问题）。
#       想要跨轮对话记忆的用例，在元信息区写 `session: shared` 显式选进去
#       （见 docs/TESTING_GUIDE.md §13.1，参考 tests/cases/session/）——本用例
#       故意保持默认，它已录制的 cassette 依赖这个模式。

>>>>>>>>>>>>>>>>
我在对比两种缓存失效策略，给你两段材料：

材料 A（来源：internal-wiki/caching-ttl.md）：
"TTL-based invalidation 简单可靠，缺点是数据可能在 TTL 到期前就已经过期，
造成短暂的脏读；适合读多写少、对一致性要求不高的场景。"

材料 B（来源：internal-wiki/caching-write-through.md）：
"Write-through invalidation 在写入时同步更新/清除缓存，一致性更好，但写路径
延迟更高，且分布式场景下需要额外的失效广播机制。"

请对比这两种策略，给出一个针对"电商商品详情页缓存"场景的结论。
<<<<<<<<<<<<<<<<
回答应该明确对比两种策略的权衡,针对给定场景给出结论。应该用类似 [1]/[2] 的
编号引用材料 A/B 的说法,并在结尾给一个来源列表(对应场景系统提示词里的引用
格式要求)。不应该调用任何工具(材料是内联给的,不需要检索)。

>>>>>>>>>>>>>>>>
我做了一个技术调研，结论是：电商商品详情页缓存应该用"TTL 兜底 + 事件驱动主动
失效 + 关键字段（库存）同步失效"的混合策略——纯 TTL 在大促期间价格/库存变更
后有不可接受的脏读窗口，纯 Write-through 的写路径延迟 + 分布式失效广播成本又
太高。请把这个结论写进一个笔记文件，方便我后续调研时参考。
<<<<<<<<<<<<<<<<
模型应该调用 Write 工具创建一个笔记文件(比如 research_notes.md),内容包含上面
给出的结论。不应该调用 Bash/Edit/NotebookEdit(这个场景里不可用)。
