# 测试用例：Skills —— 启动时加载的项目 skill 能被真实调用
# 前置: --fixture tests/fixtures/template_project --scene coding
# 预期: 验证 `.agents/skills/code-review/SKILL.md`（子目录格式）在全新进程启动的第一个
#       session 里就已经被扫描进 SkillManager、出现在系统提示词的 Available Skills 里，
#       并且模型能通过 Skill 工具真的把它调用成功（不是 "not found"）。
#       回归目标：crates/skills/src/manager.rs 的 load_dir → load_dir_subdirs 修复
#       （见 docs/design/2026-08-05-test-architecture.md §16）——子目录格式的项目 skill
#       之前在启动时被完全无视，只有等它第一次被文件监听器命中才会进内存。

>>>>>>>>>>>>>>>>
不用读任何文件，直接告诉我：调用一下 code-review 这个 skill，看看它是干什么用的。
<<<<<<<<<<<<<<<<
模型调用了 Skill 工具，skill 参数为 "code-review"（不是报错 "not found"），随后基于加载到的
skill 内容（审查改动文件、正确性/架构问题、运行测试、按严重程度汇报）转述了这个 skill 是做
什么用的，而不是说这个 skill 不存在或无法使用。
