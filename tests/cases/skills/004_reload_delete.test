# 测试用例：Skills —— 会话中途删除的 skill 文件，下一轮不重建会话就发现它没了
# 前置: --fixture tests/fixtures/template_project --scene coding --same-session
#       （有 .mutations.json 同名 sidecar，自动切到 run_test_case_same_session）
# 预期: 第一轮 code-review 正常可用；after_turn=0 时 mutations 把
#       .agents/skills/code-review/SKILL.md 整个删掉；第二轮同一个会话（没有重建）
#       再问同样的问题，应该发现 code-review 已经不存在了——验证 SkillWatcher 对
#       嵌套在子目录里的 SKILL.md（不是顶层 skills/ 目录本身）的删除事件也能收到
#       （notify::RecursiveMode::Recursive），reload_skill() 的"path 不存在就从
#       缓存里摘掉"分支被真实触发，不是只有"新增"方向被测过。

>>>>>>>>>>>>>>>>
不用读任何文件，直接告诉我：现在有没有一个叫 code-review 的 skill 可以用？
<<<<<<<<<<<<<<<<
模型的回答表明当前存在 code-review 这个 skill（能说出它大致是干什么用的，比如审查代码/检查
正确性架构之类），没有说它不存在。
>>>>>>>>>>>>>>>>
现在再检查一下：还有没有一个叫 code-review 的 skill？
<<<<<<<<<<<<<<<<
模型这次的回答表明 code-review 已经不存在了/找不到了（比如说没有这个 skill、或者当前可用
列表里没有它），不再声称它可用，证明会话中途删除的 skill 文件在没有重建会话的情况下、下一轮
就被发现已经消失。
