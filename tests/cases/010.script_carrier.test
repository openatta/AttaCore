脚本载体 · 真实模型

绑定：`.atta/settings.json` 把 `.atta/scripts/house_style.js` 绑在 `prompt.assemble` 上，
脚本往装配好的提示词里追加一条"每条回复都要以某一行结尾"的规则。

这个用例问的不是"脚本文本有没有到达模型"——那由 `tests/runner/tests/script_carrier.rs`
用脚本化模型回答，且不花钱。这里问的是脚本对提示词的改动**有没有改变模型的行为**。

>>>>>>>>>>>>>>>>
What is 2 + 2?
<<<<<<<<<<<<<<<<
回答了 4，并且最后一行是 `-- checked by the house style script`。

>>>>>>>>>>>>>>>>
Name one primary colour.
<<<<<<<<<<<<<<<<
说出了一种原色，并且最后一行同样是 `-- checked by the house style script`——
规则在第二轮仍然生效，说明脚本每轮都跑，不是只在第一轮注入了一次。
