# 测试用例：会话连续性 —— 第二轮必须看得见第一轮的对话
# session: shared
# 前置: --scene coding（不需要 fixture）
# 预期: 第一轮只把三条信息说给模型听（不落盘、不调工具）；第二轮不再重复这些
#       信息，只问"刚才说的是什么"。回答里必须出现 ORCHID 和 4271 两个只在
#       第一轮出现过的字面量——这两个断言写成 `@contains:`，不依赖 LLM 裁判，
#       没有 --compare 也会检查。
#
# 为什么这个用例必须存在：`api_runner.rs` 默认给每一轮一个新的 session_id
#       （`test-turn-{index}`），`cli_runner.rs` 默认每轮一个新的 daemon
#       session——也就是说在此之前，仓库里**没有任何用例真正跑过跨轮对话**。
#       记忆召回、already_surfaced 去重、上下文压缩、session-memory 陈旧判定、
#       压缩后恢复，这些机制全都只在"轮与轮之间"才有行为可言，一个都没有被
#       端到端覆盖过（一次审计里发现的三个 P0 记忆 bug 就是这么在 1737 个
#       测试全绿的情况下溜过去的）。
#
#       上面那行 `# session: shared` 就是本用例的开关：解析器读到它之后，
#       整个用例跑在同一个 Agent/session 上。把那行删掉再跑，第二轮就会因为
#       看不到第一轮的对话而答不出 ORCHID/4271，@contains 断言直接失败——
#       这正是它要守住的东西。
#
# 录制/回放:
#   ATTA_VCR_RECORD=001_multi_turn_recall cargo run -p test-runner -- \
#     --mode api --case tests/cases/session/001_multi_turn_recall.test
# scene: coding

>>>>>>>>>>>>>>>>
先记住这三条信息，不要写任何文件、不要调用任何工具，只回复"已记住"：
项目代号是 ORCHID，构建命令是 make turbo，测试端口是 4271。
<<<<<<<<<<<<<<<<
模型只做了简短确认（例如"已记住"），没有调用任何工具，也没有创建/修改文件。
@no-tools

>>>>>>>>>>>>>>>>
用一行回答：我刚才告诉你的项目代号和测试端口分别是什么？直接从我们刚才的对话里
回答，不要调用任何工具，也不要去读任何文件。
<<<<<<<<<<<<<<<<
模型直接从上一轮的对话里答出了项目代号 ORCHID 和测试端口 4271，没有说"看不到
之前的对话"，也没有靠调用工具去文件系统里找。
@contains: ORCHID
@contains: 4271
@no-tools
